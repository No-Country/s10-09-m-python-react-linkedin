const { JobApplication } = require("../config/db");
const { handleHttpError } = require("../utils/handleError");

const createJobAplication = async (req, res) => {};

module.exports = { createJobAplication };
